#include<ctsdio>
#include<iostream>
using namespace std;

void initNext(char temp[], int next[]) {
    // index=0 next[index]=-1
    next[0] = -1;
    for(int j)
}

int main() {
    char master[1024]={0};
    char temp[1024]={0};
    int next[1024];
    initNext(temp, next);
}
